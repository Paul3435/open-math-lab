# Dirac's theorem (min-degree Hamiltonian cycle) — formalize-only

Authoritative pin: `catalog/problems/dirac-hamiltonian/STATEMENT.md`.

**expected:** known-classical (Dirac 1952). No novelty claim.

If `n = Fintype.card V ≥ 3` and `2 * G.minDegree ≥ n`, then
`G.IsHamiltonian` (Mathlib Hamiltonian cycle).
