# Euler's partition theorem (odd parts = distinct parts) — formalize-only

Authoritative pin: `catalog/problems/euler-odd-distinct/STATEMENT.md`.

**expected:** known-classical (Euler 1748). No novelty claim.

For every `n`, `(Nat.Partition.odds n).card = (Nat.Partition.distincts n).card`.
Proof pin: Glaisher bijection, not Archive generating functions.
