# Dilworth's theorem — min chain partition = max antichain — formalize-only

Authoritative pin: `catalog/problems/dilworth-poset/STATEMENT.md`.

**expected:** known-classical (Dilworth 1950). No novelty claim.

Finite poset: minimum chain-partition size equals maximum antichain
size (width). König `ν=τ` is the engine (PR #50 MERGED), not the claim.
Mirsky dual is out of v1 unless cheap.

**OPE-613 RECOMMENDED PRIME.** New proof layer after König B closed.
Do not re-prime `konig-bipartite` Level A/B.
