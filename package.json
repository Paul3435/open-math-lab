{
  "name": "open-math-lab",
  "version": "0.1.0",
  "private": true,
  "description": "mathforge — Open Math Lab toolkit",
  "bin": {
    "mathforge": "./bin/mathforge"
  },
  "scripts": {
    "mathforge": "python bin/mathforge",
    "status": "python bin/mathforge status",
    "seed": "python bin/mathforge catalog seed",
    "test": "python bin/mathforge catalog seed && python bin/mathforge status"
  }
}
